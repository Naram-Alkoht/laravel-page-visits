<div
    data-page-visit-tracker
    data-endpoint="{{ route('page-visits.store') }}"
    data-page-key="{{ $pageVisitData->pageKey }}"
    data-page-visit-signature="{{ $pageVisitData->signature }}"
    hidden
></div>
